// Unbranded stand-in until PreparedInvocation ships the output-preparation-material minting path.
export interface ConsumedOutputPreparationMaterial {
  readonly invocationId: string;
  readonly outputDirectory: string;
}
